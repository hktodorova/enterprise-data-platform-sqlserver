# Performance tuning notes

This folder keeps small, reproducible query tuning examples. The goal is not to benchmark a specific machine, but to document how query shape and indexing affect SQL Server execution plans.
